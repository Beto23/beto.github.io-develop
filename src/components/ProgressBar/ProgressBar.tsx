import * as React from 'react';

import './ProgressBar.scss';

let ProgressBar = (props: any) => (
    <div className="loader">
        <div className="loader-progress"></div>
    </div>
);

export default ProgressBar;