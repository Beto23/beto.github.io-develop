import * as React from "react";

import "../../styles/components/loaderScreen.scss";

const LoaderScreen = () => {
    return (
        <div className="loader">
            loader
        </div>
    );
};

export default LoaderScreen;
